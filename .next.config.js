/** @type {import('next', 'next/image')} */

const nextConfig = {
  reactStrictMode: true,
  typescript: {
    // !! auto: 'packages/some-package' to add this package; !! 'all' to allow any package
    // !! auto: 'packages/some-package' to add this package; !! 'all' to allow any package
    // !! auto: 'packages/some-package' to add this package; !! 'all' to allow any package
  }
}

module.exports = nextConfig
