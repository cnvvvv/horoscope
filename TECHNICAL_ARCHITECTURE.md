# 🏗️ 科学算命系统 - 技术架构文档

## 📊 技术架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  浏览器访问   │  │  移动端访问   │  │  API调用     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                       前端层 (Next.js)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  八字算命页面 │  │  奇门遁甲页面 │  │  主页/其他    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│         ↓                    ↓                    ↓      │
│  ┌──────────────────────────────────────────────────┐   │
│  │          React Components (UI层)               │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    API层 (Next.js API Routes)              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  八字API接口   │  │  奇门API接口   │  │  数据验证     │  │
│  │  /api/bazi   │  │  /api/qimen  │  │  中间件      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   业务逻辑层 (Lib)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  八字算命模块 │  │  奇门遁甲模块 │  │  工具函数     │  │
│  │  - 排盘算法  │  │  - 排盘算法  │  │  - 日期处理  │  │
│  │  - 五行分析  │  │  - 规则引擎  │  │  - 格式化    │  │
│  │  - 十神分析  │  │  - 评分算法  │  │  - 验证      │  │
│  │  - 大运计算  │  │  - 文案生成  │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   数据层 (Types/Data)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  类型定义     │  │  规则库       │  │  常量定义     │  │
│  │  TypeScript  │  │  JSON配置    │  │  枚举值       │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏛️ 系统架构详解

### 1. 表现层 (Presentation Layer)

#### 技术选型
- **框架**：Next.js 14 (App Router)
- **语言**：TypeScript 5.3
- **样式**：Tailwind CSS 3.4
- **组件库**：自定义组件 + Lucide React

#### 页面结构
```
app/
├── page.tsx                          # 主页
├── input/                            # 八字输入
│   └── page.tsx
├── bazi/                             # 八字结果
│   └── page.tsx
├── wuxing/                           # 五行分析
│   └── page.tsx
├── shen/                             # 十神分析
│   └── page.tsx
└── qimen/                            # 奇门遁甲
    ├── page.tsx                      # 输入页面
    └── result/page.tsx               # 结果页面
```

#### 组件设计原则
- **可复用**：通用组件抽离
- **类型安全**：完整的Props类型定义
- **响应式**：移动端优先设计
- **可访问性**：ARIA标签、键盘导航

### 2. API层 (API Layer)

#### 技术选型
- **框架**：Next.js API Routes
- **协议**：REST API
- **数据格式**：JSON

#### API设计

##### 八字算命API
```typescript
// 八字排盘
GET /api/bazi?year=2024&month=2&day=7&hour=14&gender=male&type=solar

// 五行分析
GET /api/wuxing?year=2024&month=2&day=7&hour=14&gender=male

// 十神分析
GET /api/shen?year=2024&month=2&day=7&hour=14&gender=male

// 大运计算
GET /api/dayun?year=2024&month=2&day=7&hour=14&gender=male&currentAge=30&currentYear=2024

// 天干地支关系
GET /api/relationships?year=2024&month=2&day=7&hour=14&gender=male
```

##### 奇门遁甲API
```typescript
// 奇门排盘
GET /api/qimen/pai?year=2024&month=2&day=7&hour=14

// 奇门分析
GET /api/qimen/analyze?year=2024&month=2&day=7&hour=14&category=wealth&question=xxx
```

#### 统一响应格式
```typescript
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
  message?: string;
  timestamp?: string;
}
```

### 3. 业务逻辑层 (Business Logic Layer)

#### 八字算命模块

##### 核心算法
```typescript
// 历法转换
export function solarToLunar(date: Date): LunarDate;

// 八字排盘
export function calculateBazi(birthDate: Date, hour: number, gender: Gender, type: CalendarType): Bazi;

// 五行分析
export function analyzeWuXing(bazi: Bazi): WuXingAnalysis;

// 十神分析
export function calculateShen(bazi: Bazi): ShenAnalysis;

// 大运计算
export function calculateDaYun(bazi: Bazi, currentAge: number, currentYear: number): DaYunCycle;

// 天干地支关系
export function analyzeRelationships(bazi: Bazi): RelationshipAnalysis;
```

##### 算法特点
- **精确性**：基于天文历法计算
- **完整性**：支持1900-2100年
- **标准化**：统一的数据格式
- **可测试**：独立的算法函数

#### 奇门遁甲模块

##### 核心算法
```typescript
// 排盘算法
export function paiQimenPan(year: number, month: number, day: number, hour: number): QimenPan;

// 规则匹配
export function matchRules(pan: QimenPan, category: QimenCategory): Rule[];

// 评分计算
export function calculateScore(pan: QimenPan, category: QimenCategory): ScoreResult;

// 文案生成
export function generateFullAnalysis(pan: QimenPan, category: QimenCategory, scores: ScoreResult): Analysis;
```

##### 规则引擎
```typescript
// 规则结构
interface Rule {
  id: string;
  category: QimenCategory;
  conditions: RuleCondition[];
  result: string;
  score: number;
  priority: number;
}

// 条件匹配
function matchCondition(condition: RuleCondition, pan: QimenPan): boolean;
```

### 4. 数据层 (Data Layer)

#### 类型定义
```typescript
// 八字类型
interface Bazi {
  year: BaziPillar;
  month: BaziPillar;
  day: BaziPillar;
  hour: BaziPillar;
  gender: Gender;
  name?: string;
}

// 奇门类型
interface QimenPan {
  panJu: number;
  isYinDun: boolean;
  zhiFu: ZhiFuInfo;
  zhiShi: ZhiShiInfo;
  jiuGong: GongInfo[];
  year: number;
  month: number;
  day: number;
  hour: number;
  jieQi: string;
}
```

#### 规则库
```json
{
  "category": "wealth",
  "yongShen": ["生门", "值符", "戊", "庚"],
  "rules": [
    {
      "id": "wealth_001",
      "conditions": [
        { "type": "men_gong", "operator": "equals", "value": "生门" }
      ],
      "result": "生门落宫为求财用神，当前利于求财",
      "score": 80,
      "priority": 1
    }
  ]
}
```

---

## 🔄 数据流

### 八字算命流程

```
用户输入
  ↓
表单验证
  ↓
API调用 (/api/bazi)
  ↓
参数验证
  ↓
历法转换 (solarToLunar)
  ↓
八字排盘 (calculateBazi)
  ↓
返回八字数据
  ↓
前端渲染
  ↓
展示结果
```

### 奇门遁甲流程

```
用户输入 (分类 + 问题 + 时间)
  ↓
表单验证
  ↓
API调用 (/api/qimen/analyze)
  ↓
参数验证
  ↓
奇门排盘 (paiQimenPan)
  ↓
规则匹配 (matchRules)
  ↓
评分计算 (calculateScore)
  ↓
文案生成 (generateFullAnalysis)
  ↓
返回分析结果
  ↓
前端渲染
  ↓
展示九宫格 + 分析
```

---

## 🔒 安全性

### 1. 输入验证
- 参数类型验证
- 参数范围验证
- SQL注入防护（不使用数据库）
- XSS防护（React自动转义）

### 2. API安全
- HTTPS传输
- 速率限制（可配置）
- 错误信息脱敏

### 3. 隐私保护
- 不存储用户数据
- 本地计算，不上传
- 免责声明

---

## ⚡ 性能优化

### 1. 前端优化
- **代码分割**：动态导入组件
- **图片优化**：Next.js Image组件
- **缓存策略**：浏览器缓存
- **懒加载**：组件懒加载

### 2. API优化
- **缓存策略**：Vercel KV缓存
- **计算优化**：算法优化
- **响应压缩**：Gzip压缩

### 3. 部署优化
- **CDN**：Vercel Edge Network
- **SSR**：服务端渲染
- **静态生成**：静态页面优化

---

## 🧪 测试策略

### 1. 单元测试
- 算法函数测试
- 工具函数测试
- 类型定义验证

### 2. 集成测试
- API接口测试
- 数据流测试
- 前后端集成测试

### 3. 端到端测试
- 用户流程测试
- 跨浏览器测试
- 移动端测试

---

## 📊 监控和日志

### 1. 性能监控
- Vercel Analytics
- Lighthouse评分
- Core Web Vitals

### 2. 错误监控
- 错误日志收集
- 异常追踪
- 性能日志

### 3. 用户行为分析（可选）
- 页面访问统计
- 功能使用频率
- 用户留存率

---

## 🚀 部署架构

### Vercel部署架构
```
用户 → Vercel Edge Network → Vercel Serverless Function → 代码执行
```

### 部署流程
1. 代码提交到GitHub
2. Vercel自动触发构建
3. 构建成功后自动部署
4. Edge Network全球分发

---

## 📚 技术文档索引

- `PROJECT_SUMMARY.md` - 项目总结
- `README.md` - 项目说明
- `DEPLOYMENT_GUIDE.md` - 部署指南
- `QUICKSTART.md` - 快速开始
- `QIMEN_README.md` - 奇门遁甲文档

---

*更新时间：2026-02-08*
