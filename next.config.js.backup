/** @type {import('next', 'next/image')} */

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typescript: {
    // !! auto
  },
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'horoscope.vercel.app',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'horoscope-cnvvvv.vercel.app',
        port: '',
        pathname: '/**',
      }
    ],
    unoptimized: false
  }
}

module.exports = nextConfig

module.exports = nextConfig
